Upload these files to the repository root, replacing the existing files. This is an update package, not a standalone site. The official logo is unchanged from the supplied PNG. Fixes: shared logo source, placement-based sizing, visible hero logo, navigation consistency, Arabic fallback text, English tagline, stylesheet/script cache version. JavaScript syntax and static HTML checks passed; browser preview unavailable.
